#include <iostream>
int array (){
 int maxEl;
  int arr[10];
  for (int i = 0; i < 10; ++i){
      std::cout << "array[" << i + 1 << "]" << ": ";
      std::cin >> arr[i];
   }
 maxEl = arr[0];
  for (int i = 1; i < 10; ++i){

      if (maxEl < arr[i]){
          maxEl = arr[i];
        }
    }
  std::cout << "maximum element =" << maxEl << std::endl;
  std::cout << "array=[";
  for (int i = 0; i < 10; ++i){
      std::cout << arr[i] << " ";
    }
  std::cout << "]";
  return 0;
}
#define mutq_anel_zangvac array() 
#define hashvel_max_arjeq
#define tpel ;
//int mutq_anel_zangvac = 1, hashvel_max_arjeq = 2, tpel = array ();
int main (){
  mutq_anel_zangvac
  hashvel_max_arjeq
  tpel
  return 0;
}

