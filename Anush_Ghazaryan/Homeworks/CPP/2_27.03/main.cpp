#include<iostream>

#define mutqagrel_zangvats  int size = 0;  std::cout << "Input the size of the Array : " ; std::cin >> size; int array [ size ]; std::cout << "Input the elements " << std::endl; for ( int index = 0; index < size; ++ index ) { std::cin >> array [ index ]; } 


#define hashvel_zangvatsi_mecaguyn_arjeqy int max = array [0]; for ( int index =0; index < size ; ++ index ) { if ( max < array [ index ] ) { max = array [ index ]; } } 


#define tpel_zangvatsi_mecaguyn_arjeqy std::cout << "Maximum value : " << max << std::endl; 

int main () {

	mutqagrel_zangvats
	hashvel_zangvatsi_mecaguyn_arjeqy
	tpel_zangvatsi_mecaguyn_arjeqy


  return 0;
}
