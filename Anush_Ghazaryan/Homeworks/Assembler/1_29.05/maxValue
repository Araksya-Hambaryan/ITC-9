Here is used:

maxValue(R2)- a function with the R2 argument
for[] -end for-for loop
if[]-end if - if statement
eq- equal (= or ==)
Print- prints the value of the Register
main()- main function


    
    maxValue(R2) {
	
	for[ R1 eq 0; R1 less than 5; add 1 to R1 ]
	
	    if[ R2 ${R1} greater than R0 ]
  	    	
		R0 eq R2 ${R1}
	    end if
	end for
	R0 eq R3
	Print R3		
    }

    main() {
	
        array[5] eq [10,111,5,1,8]
	R2 eq array
	maxValue(R2)
}
