
Here is used:

Input - for inputing the value of the variable
eq- equal (= or ==)
Print-outputs,prints the value of the Register
for[]- end for  -for loop
if[]- end if   -if statement
fib(R3)- simple function printing the fibonacci series (R3 is the argument)
main()-main function
$n- variable or given number ($n , $i)
less than - <
add ... to ... -  ...+... (adds 2 Registers)
continue- goes on  next action 

    
    fib(R3) {

	R2 eq 0
	R1 eq 1
	R0 eq 0

	for[ $i eq 1; $i less than R3; add 1 to $i ]

	
		if[ $i eq 1 ]
			Print (R2)
			continue
		end if

		if[ $i eq 2 ]
			Print (R1)
			continue
		end if
	end for
		R0 eq add R2 to R1
		R2 eq R1
		Print (R0)
    }

    main {
	Print("Enter n : ")
	Input n
	fib $n
    }

