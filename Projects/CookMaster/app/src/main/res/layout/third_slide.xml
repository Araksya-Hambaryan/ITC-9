<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorSlide_1">

    <TextView
        android:id="@+id/textView"
        android:layout_width="@dimen/layWidSize"
        android:layout_height="@dimen/layBtmS"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="170dp"
        android:gravity="center_horizontal"
        android:text="@string/bon_appetit"
        android:textColor="@color/white"
        android:textSize="@dimen/txtSizeslide3"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/imageView3"
        android:layout_width="@dimen/imageSize"
        android:layout_height="@dimen/imageSize"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/margTop"
        app:srcCompat="@drawable/cook" />

</RelativeLayout>